<%= turbo_stream.replace "follow_button_User_#{@user.id}" do %>
  <%= button_to t('profiles.show.follow'), user_follows_path(@user), method: :post, id: "follow_button_User_#{@user.id}", class: "btn btn-primary" %>
<% end %>
