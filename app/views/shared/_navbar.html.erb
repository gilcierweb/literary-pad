<nav class="bg-white shadow-sm border-b">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between h-16">
      <div class="flex">
        <%= link_to root_path, class: "flex items-center" do %>
          <span class="text-2xl font-bold text-primary">📚 StoryHub</span>
        <% end %>

        <div class="hidden sm:ml-8 sm:flex sm:space-x-6">
          <%= link_to "Discover", discover_path, class: "inline-flex items-center px-1 pt-1 text-sm font-medium text-gray-700 hover:text-primary" %>
          <%= link_to "Stories", stories_path, class: "inline-flex items-center px-1 pt-1 text-sm font-medium text-gray-700 hover:text-primary" %>
        </div>
      </div>

      <div class="flex items-center space-x-4">
        <% if user_signed_in? %>
          <%= link_to "Dashboard", dashboard_path, class: "text-sm font-medium text-gray-700 hover:text-primary" %>
          <%= link_to "Write", new_story_path, class: "btn-primary" %>

          <div class="relative" data-controller="dropdown">
            <button type="button" class="flex items-center text-sm font-medium text-gray-700 hover:text-primary" data-action="click->dropdown#toggle">
              <%= current_user.profile.display_name %>
              <svg class="ml-1 h-5 w-5" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M5.293 7.293a1 1 0 011.414 0L10 10.586l3.293-3.293a1 1 0 111.414 1.414l-4 4a1 1 0 01-1.414 0l-4-4a1 1 0 010-1.414z" clip-rule="evenodd" />
              </svg>
            </button>

            <div data-dropdown-target="menu" class="hidden absolute right-0 mt-2 w-48 bg-white rounded-lg shadow-lg py-1 z-10">
              <%= link_to "My Profile", profile_path(current_user), class: "block px-4 py-2 text-sm text-gray-700 hover:bg-gray-100" %>
              <%= link_to "Settings", edit_profile_path(current_user), class: "block px-4 py-2 text-sm text-gray-700 hover:bg-gray-100" %>
              <hr class="my-1">
              <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "block w-full text-left px-4 py-2 text-sm text-red-600 hover:bg-gray-100" %>
            </div>
          </div>
        <% else %>
          <%= link_to "Sign In", new_user_session_path, class: "text-sm font-medium text-gray-700 hover:text-primary" %>
          <%= link_to "Sign Up", new_user_registration_path, class: "btn-primary" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>