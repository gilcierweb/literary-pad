<%= turbo_stream.replace "like_button_#{@likeable.class.name}_#{@likeable.id}" do %>
  <%= button_to like_path, id: "like_button_#{@likeable.class.name}_#{@likeable.id}", class: "btn btn-ghost flex-1" do %>
    🤍 Like
  <% end %>
<% end %>